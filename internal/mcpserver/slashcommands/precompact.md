---
description: Recover the messages immediately preceding the last /compact event
---

Call the `mcp__klyne__get_pre_compact_context` MCP tool with no arguments — let it auto-resolve the session from the current working directory.

Display the `markdown` field of the response VERBATIM. The server has already rendered the "no compact yet" message, the ambiguous-candidates list, or the recovered messages with their roles and timestamps as appropriate.

Do not summarise, paraphrase, or add commentary — output the markdown field byte-for-byte and stop.
