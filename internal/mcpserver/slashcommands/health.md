---
description: Classify the current session's context health and list the top bloat sources
---

Call the `mcp__klyne__get_context_health` MCP tool with no arguments — let it auto-resolve the session from the current working directory.

Display the `markdown` field of the response VERBATIM. The server has already rendered the verdict, recommended action, reason, fill percentage, and top bloat sources.

Do not summarise, paraphrase, or editorialize — output the markdown field byte-for-byte and stop.
