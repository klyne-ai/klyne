---
description: Per-turn token usage for the active session — sparkline, % of context window, and cached vs uncached split
---

Call the `mcp__klyne__get_token_timeline` MCP tool with no arguments — let it auto-resolve the session from the current working directory.

Display the `markdown` field of the response VERBATIM. The server has already rendered the headline trajectory, the ASCII sparkline, the per-turn table, the activity heatmap, and the ambiguous-candidates list when applicable.

Do not summarise, paraphrase, reformat, or add commentary — output the markdown field byte-for-byte and stop.
