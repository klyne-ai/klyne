---
description: Day-1 session brief — recent sessions, project + global memories, latest health verdict
---

Call the `mcp__klyne__bootstrap` MCP tool with no arguments — let it auto-resolve the project from the current working directory.

Display the `markdown` field of the response VERBATIM. The server has already rendered all four sections (recent sessions, project memories, global memories, current session health) with `_(none)_` placeholders for empty ones so the shape is stable.

Do not summarise, paraphrase, or editorialize — output the markdown field byte-for-byte and stop.
